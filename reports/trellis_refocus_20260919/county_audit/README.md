# Trellis county/state refocus audit

Audit is offline and source-bound; no network requests or publication changes. `report.json` holds the timestamp and exact counts. `next_10_observed_urls.jsonl` supplies ten unattempted literal URLs with link labels, parent-page records, source-file SHA-256 and frontier evidence.

The legacy observed county URL candidate set is 2,508. There are 1,432 catalog profiles and six separately validated/published browser profiles: 1,438 saved, 1,070 remaining (57.34% saved). This is not a denominator of Census counties or every possible Trellis page. One known candidate, `/coverage/texas/harrisoncountytexas.org`, is a county-website link artifact and should be excluded from future queues. After that explicit exclusion the candidate denominator is 2,507 and the gap is 1,069. The earlier 1,073-gap note omitted the last three browser captures. All 45 observed state-page URLs are already saved.

The new free-coverage folder has 29 response files: 27 state responses and two usage checks. Twenty-six state responses lack connector errors; Alabama has an error. Their 1,481 county rows are publisher-listed coverage entries, not new downloaded profiles. The 33-entry call log references four absent state response files (IA, KS, KY, ME). A separate Middlesex, NJ county-detail scout response exists. `trellis_coverage.py` and a uniform validation envelope were absent at audit time; `test_trellis_coverage.py` alone was only a design fixture. Some state files were reconstructed by compact transcription; distinguish them from directly saved connector wrapper bytes. Neither is original HTTP content.

Profile quality: 1,429/1,432 have population; 1,419 county seat; 1,276 phone; 1,366 administration address; 1,361 name meaning. There are 1,384 canonical website hrefs, 45 missing websites and three invalid/internal links. Display URLs are truncated in 461 records: use the observed href field and its provenance, not visible ellipses. Two profiles have empty field objects; all 1,432 source files exist. Raw titles need whitespace cleanup. Extraneous page headings also appear as sparse field keys, so use an explicit known-field whitelist and retain other content as separately labeled text. Every field is a publisher claim, not current authoritative verification.

For browser acquisition, reuse the structure of `sources/counties/trellis_browser_backfill_20260918T2213/save_observations.py` in a NEW timestamped folder. Parameterize state and selection manifest, include every previous browser manifest in overlap checks, and keep original frozen batches unchanged. Capture visible profile heading, breadcrumb, h2/h3 fields and hrefs; retain signed-in/access observations without claiming paid-document entitlement. Match county heading + state + observed parent label exactly to the county inventory; do not infer GEOID from URL slug. Stop at login/CAPTCHA/access barriers.

Root integration path: register the new folder in `server.TRELLIS_BROWSER_BATCHES`; `load_trellis_browser_batches()` validates manifest/summary/receipt consistency, DOM/text/metadata hashes and namespace. The locked directory builder additionally checks exact inventory GEOID/state/name and profile overlap. Existing query route `/api/counties` and `/api/documents` dataset `trellis_browser_counties` consume the result. Do not trigger a full law-text rebuild merely to publish county metadata. Free connector normalization should be a distinct uniform-gated supplement and keep documents/courthouses coverage flags separate from saved-file or completeness claims.

Next selected county URLs: Texas/Jasper; Virginia/King and Queen; Georgia/Jenkins; Missouri/Lewis; Kansas/Lane; Iowa/Iowa; North Carolina/Jackson; Indiana/Lake; Minnesota/Lyon; Ohio/Lawrence. Exact URLs and parent hashes are in the JSONL. No missing state URL is invented; missing connector responses are a separate metadata gap.
