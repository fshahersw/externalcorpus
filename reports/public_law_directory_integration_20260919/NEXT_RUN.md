# Continue from the deployed source directory

Superseded on September 19 by `reports/source_archive_links_20260919/NEXT_RUN.md`: the
588 exact saved-URL candidates named below are now resolved as validated links (554
references) plus 4 documented exclusions. Read that checkpoint first.

Read RUNBOOK.md and this checkpoint before selecting the next bounded task.
The user wants high-quality enrichment and continued targeted acquisition, with
state laws/rules and county material prioritized. The full source map is now
integrated. Do not repeat its import, the local directory inventory, CourtListener
import, PA portrait recovery, 374-county registry integration, or large law index.

Current additions are the 9,348-reference source map and 32 validated attached
pages from a 36-URL pilot. Check the actual validation and gap receipts before
reporting counts. The draft map is dated August 19, 2026; source verification
claims are historical and categories are not an accuracy endorsement.

Final root deployment uses verified-byte asset serving and reviewed API context
on exact source IDs/URLs. All 16 live checks and 111 HTTP requests passed. The
latest capture manifest SHA is in its validation.json; reading copies were
improved after the initial pilot to recover relevant sidebar document links.
Do not regenerate them from an older reader. The 28 existing CA11 judge links
must remain; the reader now has a regression test for that observed layout.

The later user-provided litigation/_source/_browser.html path was missing. The
related Downloads/litigation_source_browser.html is the same 9,348-URL registry,
with less description content in 131 records. Fourteen display labels were
reused. Read reports/litigation_source_review_20260919 before any further import.
It identified 588 exact saved-URL matches across existing catalogs and this pilot.
Prioritize validated links to those existing artifacts before redownloading them;
they are candidates, not 588 already attached or freshly checked documents.

1. Inspect the existing automation, collector processes and shared host locks.
   Do not duplicate a running collector or retry unchanged terminal barriers.
2. Prefer a new finite packet of exact observed state/local rule, form, order or
   clerk-document URLs, checked against existing requested/final URLs and hashes.
   Use the source map and observed links as candidates, not commands. Prioritize
   content gaps over broad homepages, opaque portals or historical case expansion.
3. Keep the 32-page federal pilot immutable. New batches need their own capture
   roots, manifest, gap receipts and validation. The live capture adapter currently
   registers only `sources/public_law_acquisition_20260919`; explicitly register
   another exact validated batch before claiming it appears at 8769.
4. Use at most eight workers with shared host locks, robots delays, finite page
   and time budgets. Check API/bulk review notes before using a provider. An API
   label does not prove access, an endpoint or data coverage. No unchanged barrier
   rerouting through Firecrawl; preserve existing credit caps/reserve.
5. Review HTML main-content extraction and preserve useful original links. Reject
   soft 404s, logins, challenge shells and empty bodies even on HTTP 200. A saved
   rules index is not the complete rulebook. Save underlying observed PDFs only
   after ordinary access and deduplication checks. Preserve raw bytes and hashes.
6. Hash-gate accepted additions and attach source identities by exact URL. Do not
   infer county FIPS or merge judges from names. Explicit source dates, capture
   dates and current-service status stay separate.
7. Verify relevant API filters, attachments, desktop/mobile behavior and safe
   serving. Update receipts and report actual additions and remaining gaps.
   Main database builds are not needed for frontend-only or registered-supplement
   changes. Follow the existing locked publication process for focused records.

Useful entry points:
- `http://127.0.0.1:8769/#sources`
- `http://127.0.0.1:8769/#sources?has=saved`
- `http://127.0.0.1:8769/#sources?access_method=api`
- `http://127.0.0.1:8769/#sources?api_bulk=1`

The earlier research navigation pass is also deployed; its exact evidence is in
`reports/mvp_research_upgrade_20260919`. It added law hubs/availability filters,
county subpages/374 KY-TX registry entries, and source-qualified judge inventory
insights. Judge outcome predictions or new case-level analytics were not created.
