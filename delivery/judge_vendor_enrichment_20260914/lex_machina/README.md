This component contains one public, publisher-reported judge measurement: **James Rodney Gilstrap was assigned 2,276 patent-related cases during 2023–2025**. It comes from a LexisNexis/Lex Machina article published August 11, 2026 and directly retrieved September 14, 2026 with HTTP 200 and verified TLS.

`observations.jsonl`, `facts.jsonl`, and `analyses.jsonl` retain the native observation ID, original HTML/text/metadata paths and SHA-256 hashes. HTML character offsets address decoded UTF-8 after universal CRLF/CR-to-LF conversion, matching the component schema. Separate byte offsets and a raw-fragment hash address the unchanged original bytes. The analysis is a case-assignment count, not a win rate, grant rate, current caseload, or judge-quality score. Unknown denominators, current service, court and state remain null or empty. The federal classification comes from the article's explicit federal district-court patent discussion; no particular court is inferred from adjacent venue statistics.

Five public HTML documents were downloaded. The other four document product features, report-request access, API structure and published terms; they are not judge observations. `source_documents.jsonl` lists all five. No credentials, forms, licensed feed, private APIs, paid credits, case files or existing sealed corpora were used or changed.

The old public PDF host did not resolve in this environment. Historical PDF table candidates are retained separately under `sources/judges/vendor_probe_20260914/lex_machina/` and are excluded from this component. The full 2026 report was not obtained. The saved article does not expose its underlying case IDs or assignment/deduplication methodology.

Rebuild offline from the workspace root using the bundled Python executable and `delivery/judge_vendor_enrichment_20260914/lex_machina/build.py`. `validation.json` binds every semantic output and the original files. This dataset does not establish a unique-current-judge count or national completeness.
